# Changelog

All notable changes to Transient Recall release artifacts are documented here.

This project follows semantic versioning for release tags.

## [Unreleased]

- Define public AGPL source mirror release policy.
- Add CI release smoke gate.
- Add backup/restore runbook for continuity durability.
- Add release runbook for pinned tags and checksum publishing.

## Release entry template

Each version entry should include:

- `Added` / `Changed` / `Fixed` bullets
- migration notes (if any)
- install artifact references (image tag + checksum manifest)
